# Robot simulator

L ← ⊙(×¯1_1↻1)
R ← ⊙(×1_¯1↻1)
A ← +,

Start ← 7_3
Dir ← 0_1
Instr ← "RAALAL"

# Select an action based on the character
Step ← (
  ⊗□: {@A @L @R}
  ⨬(A|L|R|∘)
)

Instr Start Dir
◌⍥⊃(↘1|Step ⊢)⧻.
